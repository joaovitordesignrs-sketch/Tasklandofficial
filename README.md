
  # Taskland Alpha version

  This is a code bundle for Taskland Alpha version. The original project is available at https://www.figma.com/design/kB25PuNta1V9Pfx3V1y1fr/Taskland-Alpha-version.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  