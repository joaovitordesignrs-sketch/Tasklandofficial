import { ComingSoonOverlay } from "./ui/ComingSoonOverlay";

/**
 * ClassSelectionScreen — currently behind "Coming Soon".
 * Full UI is in git history; restore when the feature is enabled.
 */
export default function ClassSelectionScreen() {
  return <ComingSoonOverlay minHeight={400} />;
}
